spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/headhunter
    username: haxul
    password: haxul
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml